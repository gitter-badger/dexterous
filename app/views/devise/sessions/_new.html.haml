= form_for resource, as: resource_name, url: session_path(resource_name), html: {class: 'pure-form pure-form-stacked', id: 'login-form'} do |f|
    %fieldset
        = f.label :email
        = f.email_field :email
        = f.label :password
        = f.password_field :password
        = f.submit "Login", class: 'pure-button pure-button-primary', id: 'login-submit'
