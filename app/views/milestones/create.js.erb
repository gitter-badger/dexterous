$('.sjs--milestones--index--outlet').html(
    "<%= j render 'tracks/milestone_list', track: milestone.track, milestone: milestone, format: :html %>" + 
    "<%= j render 'tracks/new_milestone_entry', track: milestone.track, milestone: milestone, format: :html %>"
);
