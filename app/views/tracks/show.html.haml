= render 'tracks/header'

- if track.milestones.size > 0
  #timeline
    #spine
    #milestones.sjs--milestones--index--outlet
      = render 'milestone_list', track: track
- else
  .zilch-wrapper
    .smiley :(
    .message Nothing to see here yet. Move along!
